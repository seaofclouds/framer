%h2 Use lorem ipsum for placeholder text
  
.demo.container
  .example
    %h3= lorem(:title, 1)
    %p
      by
      = name(:full)
      (#{name(:user)})
    = lorem(:paragraph, 3)
  .snippet
    %textarea
      :preserve
        %h3= lorem(:title, 1)
        %p
          by
          = name(:full)
          = name(:user)
        = lorem(:paragraph, 3)